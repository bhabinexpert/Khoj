# Khoj ml-service (FastAPI): CV parsing + CV-to-job match scoring

fastapi==0.115.6
# starlette must stay in this range to satisfy fastapi==0.115.6.
# starlette 1.x (a breaking major-version bump) was found installed on this
# machine and caused TypeError: Router.__init__() got an unexpected keyword
# argument 'on_startup', crashing the service on every start.
starlette>=0.40.0,<0.42.0
uvicorn[standard]==0.34.0
pydantic==2.10.5
python-multipart==0.0.20

# Document text extraction
pdfplumber==0.11.5
python-docx==1.1.2

# Semantic skill matching. Heavy (pulls in torch, ~2.5 GB) but it is what makes
# "ReactJS" match "React". The service degrades to a deterministic lexical
# matcher when this is absent — see app/matching/embedder.py.
sentence-transformers==3.3.1

# Dev / test
pytest==8.3.4
httpx==0.28.1
reportlab==4.2.5
