# Khoj ml-service — LEAN runtime deps (no torch).
#
# This is the free-tier / lexical install: FastAPI + the document parsers, and
# nothing else. With sentence-transformers absent, app/matching/embedder.py
# automatically falls back to the deterministic LexicalMatcher, so CV parsing
# and skill scoring both work — just without semantic similarity.
#
# To run the semantic matcher instead, install requirements.txt (pulls ~2.5 GB
# of torch) on a >=2 GB instance and set EMBEDDINGS_ENABLED=true. No code change.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
python-multipart==0.0.20

# Document text extraction
pdfplumber==0.11.5
python-docx==1.1.2
